% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wa_dropdown.R
\name{wa_dropdown}
\alias{wa_dropdown}
\title{Create a \code{wa-dropdown} component}
\usage{
wa_dropdown(
  input_id,
  ...,
  class = NULL,
  style = NULL,
  dir = NULL,
  distance = NULL,
  lang = NULL,
  open = NULL,
  placement = NULL,
  size = NULL,
  skidding = NULL,
  trigger = NULL
)
}
\arguments{
\item{input_id}{Shiny input id for the component. This is also used as
the rendered DOM \code{id} attribute.}

\item{...}{Child content for the component's default slot.}

\item{class}{Optional CSS class string.}

\item{style}{Optional inline CSS style string.}

\item{dir}{String. Optional Web Awesome attribute.}

\item{distance}{Number. Default: \code{0}. The distance of the dropdown menu
from its trigger.}

\item{lang}{String. Optional Web Awesome attribute.}

\item{open}{Boolean. Default: \code{FALSE}. Opens or closes the dropdown.}

\item{placement}{Enumerated string. Allowed values: \code{bottom},
\code{bottom-end}, \code{bottom-start}, \code{left}, \code{left-end}, \code{left-start},
\code{right}, \code{right-end}, \code{right-start}, \code{top}, \code{top-end}, \code{top-start}.
Default: \code{bottom-start}. The placement of the dropdown menu in
reference to the trigger. The menu will shift to a more optimal
location if the preferred placement doesn't have enough room.}

\item{size}{Enumerated string. Allowed values: \code{large}, \code{medium},
\code{small}. Default: \code{medium}. The dropdown's size.}

\item{skidding}{Number. Default: \code{0}. The offset of the dropdown menu
along its trigger.}

\item{trigger}{The element that triggers the dropdown, such as a
\verb{<wa-button>} or \verb{<button>}.}
}
\value{
An HTML tag for the component.
}
\description{
Generated wrapper for the Web Awesome \code{wa-dropdown} component. When
used as a Shiny input, action semantics are exposed through
\verb{input$<input_id>} and the latest selected dropdown item value is
exposed through \verb{input$<input_id>_value}. The action input increments
on every selection, including repeated selection of the same item. The
companion value input reflects the selected item's \code{value}, returns
\code{NULL} when the selected item has no \code{value}, and preserves an explicit
empty string \code{""} when that is the selected item's value. Each
committed action publishes a numeric action value.
Generated from Web Awesome metadata.
}
\section{Shiny Bindings}{

\verb{input$<input_id>} uses action semantics and invalidates on each
committed action, including repeated selection of the same item. The
Shiny action value is returned as a numeric action value.

\verb{input$<input_id>_value} reflects the latest selected item's \code{value}
value, returns \code{NULL} when the selected item has no \code{value} , and
preserves an explicit empty string \code{""} when that is the selected
item's value. The payload value is returned as a character string or
\code{NULL}.
}

