% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wa_dropdown.R
\name{wa_dropdown}
\alias{wa_dropdown}
\title{Create a \code{wa-dropdown} component}
\usage{
wa_dropdown(
  input_id,
  ...,
  class = NULL,
  style = NULL,
  dir = NULL,
  distance = NULL,
  lang = NULL,
  open = NULL,
  placement = NULL,
  size = NULL,
  skidding = NULL,
  trigger = NULL
)
}
\arguments{
\item{input_id}{Shiny input id for the component. This is also used as
the rendered DOM \code{id} attribute.}

\item{...}{Child content for the component's default slot.}

\item{class}{Optional CSS class string.}

\item{style}{Optional inline CSS style string.}

\item{dir}{Optional Web Awesome attribute.}

\item{distance}{The distance of the dropdown menu from its trigger.
Defaults to \code{0} when omitted.}

\item{lang}{Optional Web Awesome attribute.}

\item{open}{Opens or closes the dropdown. Defaults to \code{false} when
omitted.}

\item{placement}{The placement of the dropdown menu in reference to the
trigger. The menu will shift to a more optimal location if the
preferred placement doesn't have enough room. Must be one of
\code{"bottom"}, \code{"bottom-end"}, \code{"bottom-start"}, \code{"left"}, \code{"left-end"},
\code{"left-start"}, \code{"right"}, \code{"right-end"}, \code{"right-start"}, \code{"top"},
\code{"top-end"}, \code{"top-start"}. Defaults to \code{bottom-start} when omitted.}

\item{size}{The dropdown's size. Must be one of \code{"large"}, \code{"medium"},
\code{"small"}. Defaults to \code{medium} when omitted.}

\item{skidding}{The offset of the dropdown menu along its trigger.
Defaults to \code{0} when omitted.}

\item{trigger}{The element that triggers the dropdown, such as a
\verb{<wa-button>} or \verb{<button>}.}
}
\value{
An HTML tag for the component.
}
\description{
Generated wrapper for the Web Awesome \code{wa-dropdown} component. When
used as a Shiny input, action semantics are exposed through
\verb{input$<input_id>} and the latest selected dropdown item value is
exposed through \verb{input$<input_id>_value}. The action input increments
on every selection, including repeated selection of the same item. The
companion value input reflects the selected item's \code{value}, returns
\code{NULL} when the selected item has no \code{value}, and preserves an explicit
empty string \code{""} when that is the selected item's value.
Generated from Web Awesome metadata.
}
