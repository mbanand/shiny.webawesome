% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wa_select.R
\name{wa_select}
\alias{wa_select}
\alias{update_wa_select}
\title{Create a \code{wa-select} component}
\usage{
wa_select(
  input_id,
  ...,
  class = NULL,
  style = NULL,
  value = NULL,
  disabled = NULL,
  label = NULL,
  hint = NULL,
  name = NULL,
  appearance = NULL,
  custom_error = NULL,
  dir = NULL,
  lang = NULL,
  max_options_visible = NULL,
  multiple = NULL,
  open = NULL,
  pill = NULL,
  placeholder = NULL,
  placement = NULL,
  required = NULL,
  size = NULL,
  with_clear = NULL,
  with_hint = NULL,
  with_label = NULL,
  clear_icon = NULL,
  end = NULL,
  expand_icon = NULL,
  hint_slot = NULL,
  label_slot = NULL,
  start = NULL
)

update_wa_select(
  session,
  input_id,
  value = NULL,
  label = NULL,
  hint = NULL,
  disabled = NULL
)
}
\arguments{
\item{input_id}{Input id of the component to update.}

\item{...}{Child content for the component's default slot.}

\item{class}{Optional CSS class string.}

\item{style}{Optional inline CSS style string.}

\item{value}{Optional value to send to the component.}

\item{disabled}{Optional logical disabled state to send to the component.}

\item{label}{Optional label text to send to the component.}

\item{hint}{Optional hint text to send to the component.}

\item{name}{String. Default: \code{""}. The name of the select, submitted as
a name/value pair with form data.}

\item{appearance}{Enumerated string. Allowed values: \code{filled},
\code{filled-outlined}, \code{outlined}. Default: \code{outlined}. The select's visual
appearance.}

\item{custom_error}{String. Default: \code{null}. Optional Web Awesome
attribute.}

\item{dir}{String. Optional Web Awesome attribute.}

\item{lang}{String. Optional Web Awesome attribute.}

\item{max_options_visible}{Number. Default: \code{3}. The maximum number of
selected options to show when \code{multiple} is TRUE. After the maximum,
"+n" will be shown to indicate the number of additional items that are
selected. Set to 0 to remove the limit.}

\item{multiple}{Boolean. Default: \code{FALSE}. Allows more than one option
to be selected.}

\item{open}{Boolean. Default: \code{FALSE}. Indicates whether or not the
select is open. You can toggle this attribute to show and hide the
menu, or you can use the \code{show()} and \code{hide()} methods and this
attribute will reflect the select's open state.}

\item{pill}{Boolean. Default: \code{FALSE}. Draws a pill-style select with
rounded edges.}

\item{placeholder}{String. Default: \code{""}. Placeholder text to show as a
hint when the select is empty.}

\item{placement}{Enumerated string. Allowed values: \code{bottom}, \code{top}.
Default: \code{bottom}. The preferred placement of the select's menu. Note
that the actual placement may vary as needed to keep the listbox inside
of the viewport.}

\item{required}{Boolean. Default: \code{FALSE}. The select's required
attribute.}

\item{size}{Enumerated string. Allowed values: \code{large}, \code{medium},
\code{small}. Default: \code{medium}. The select's size.}

\item{with_clear}{Boolean. Default: \code{FALSE}. Adds a clear button when
the select is not empty.}

\item{with_hint}{Boolean. Default: \code{FALSE}. Only required for SSR. Set
to \code{TRUE} if you're slotting in a \code{hint} element so the server-rendered
markup includes the hint before the component hydrates on the client.}

\item{with_label}{Boolean. Default: \code{FALSE}. Only required for SSR. Set
to \code{TRUE} if you're slotting in a \code{label} element so the
server-rendered markup includes the label before the component hydrates
on the client.}

\item{clear_icon}{An icon to use in lieu of the default clear icon.}

\item{end}{An element, such as \verb{<wa-icon>}, placed at the end of the
combobox.}

\item{expand_icon}{The icon to show when the control is expanded and
collapsed. Rotates on open and close.}

\item{hint_slot}{Text that describes how to use the input.
Alternatively, you can use the \code{hint} attribute.}

\item{label_slot}{The input's label. Alternatively, you can use the
\code{label} attribute.}

\item{start}{An element, such as \verb{<wa-icon>}, placed at the start of
the combobox.}

\item{session}{Shiny session object.}
}
\value{
An HTML tag for the component.

Invisibly returns \code{NULL}.
}
\description{
Generated wrapper for the Web Awesome \code{wa-select} component. When used
as a Shiny input, \verb{input$<input_id>} reflects the component's current
\code{value} value. The Shiny value is returned as a character string for
single-select usage, or a character vector when \code{multiple} is \code{TRUE}.
Generated from Web Awesome metadata.

Generated update helper for the Web Awesome \code{wa-select} component.
}
\section{Shiny Bindings}{

\verb{input$<input_id>} reflects the component's current \code{value} value. The
Shiny value is returned as a character string for single-select usage,
or a character vector when \code{multiple} is \code{TRUE}.
}

